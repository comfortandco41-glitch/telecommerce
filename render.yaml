services:
  # Backend API Server
  - type: web
    name: superbot-backend
    env: node
    plan: starter # Starter plan or free plan
    buildCommand: npm install --legacy-peer-deps && npx prisma generate && npx prisma db push && npm run build
    startCommand: node dist/src/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        sync: false # Enter your frontend Render static site URL (e.g., https://telecommerce-1.onrender.com)
      - key: DATABASE_URL
        sync: false # You will enter your Supabase Connection Pooler URL during setup
      - key: JWT_SECRET
        generateValue: true # Render will automatically generate a secure key for you
      - key: SUPABASE_URL
        sync: false # You will enter your Supabase URL
      - key: SUPABASE_KEY
        sync: false # You will enter your Supabase Publishable Key

  # Frontend Dashboard
  - type: web
    name: superbot-dashboard
    env: static
    buildCommand: npm install --legacy-peer-deps --prefix dashboard && npm run build --prefix dashboard
    publishPath: dashboard/dist
    routes:
      # React Router client-side routing fallback rule
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: superbot-backend
          property: host
